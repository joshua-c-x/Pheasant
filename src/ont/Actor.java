package ont;
import ecs.EntityContainer;
import eng.*;

public class Actor extends Ontic
{  
	public Actor(int entity, EntityContainer entityContainer) 
	{
		super(entity, entityContainer);
	}	  
	public void Update(IO io, float delta) { }
}
